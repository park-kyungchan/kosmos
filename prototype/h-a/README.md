# H-A PoC: Clean-slate v1.0 — Struct + ValueType + home-ontology shim

Validates ForwardProp chain: `schemas primitives → home-ontology-shim → project usage`.

```
bunx tsc --noEmit --project prototype/h-a
```
