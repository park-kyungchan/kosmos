# H-B PoC: Incremental v0.3 — additive primitives with v0.2 backcompat

Validates: Struct + ValueType added additively; v0.2 consumers compile unchanged.

```
bunx tsc --noEmit --project prototype/h-b
```
